class User {
  final int? id, zipcode;
  final String? username, email, name, address, street, suite;

  User({
    this.id,
    this.username,
    this.email,
    this.name,
    this.address,
    this.street,
    this.suite,
    this.zipcode,
  });
}
